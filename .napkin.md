# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-02-27 | bug | lake_app.py C001 tab filtered reviewed reaches using only `st.session_state.pending_fixes` (lost on refresh/restart) — C004 tab correctly queried `lint_fix_log` | Always query lint_fix_log for reviewed reaches (survives restarts), merge with session state for instant feedback |
| 2026-02-23 | self | Ran psql -f with relative path in worktree, schema didn't apply | Always use absolute paths for psql -f in worktrees |
| 2026-02-23 | self | PG schema had SERIAL PRIMARY KEY on facc_fix_log but DuckDB sends NULLs for that column | Use plain INTEGER, no PK constraint when mirroring DuckDB tables with auto-generated IDs |
| 2026-02-23 | self | v17c CALCULATE_DIST_OUT (Jan 22) corrupted 74,006 reaches (29.76%) — BFS flag heuristic at bifurcations uses max(available) when one child is -9999, never revisits | Never recalculate dist_out — use v17b values. v17c should only add hydro_dist_out (Dijkstra) as a new column |

## User Preferences
- Keep lint checks as WARNING when expected violations exist; add exclusion logic with clear comments explaining why some are allowed
- Concise plan mode output
- Worktree dmux rule: no git writes (commit, push, branch, PR, merge) but GitHub issue interactions (comment, close, open, label) are fine

## Patterns That Work
- Query v17b alongside v17c to determine whether issues are inherited or introduced
- Split lint violations into "expected" vs "unexpected" categories with a `violation_type` column in details DataFrame

## Patterns That Don't Work
- **MERIT DEM WSE as a flow-direction signal**: Always has data so dominates multi-signal votes, but is low-quality on flat rivers. SWOT is better but sparse. When they disagree, SWOT is usually right (AS 18081).
- **OLS for node-level WSE slope**: Outlier clusters (e.g., 14 bad SWOT nodes) dominate. Use Theil-Sen instead.
- **Automated flow-direction flipping at scale**: 20 approved out of 2,032 candidates (1%). Most WSE inversions aren't from reversed edges — they're DEM artifacts, lake interactions, backwater, or tidal effects.

## Mistakes This Session (is_mainstem_edge fix)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-13 | self | Post-hoc SQL fix for ghost mainstem (1,588 reaches) wasn't made durable in pipeline code. Rerun would reintroduce bug | Always fix the pipeline code, not just the database. Post-hoc SQL is a band-aid |
| 2026-03-13 | self | `compute_best_headwater_outlet` still had `overrides` parameter and v17c_pipeline.py:482 still passed overrides to it. This was the exact code path that corrupted 12,272 reaches — never removed after the incident | Remove dangerous parameters entirely. Add a warning docstring explaining WHY it was removed so nobody re-adds it |
| 2026-03-13 | self | CLAUDE.md said "subnetwork_id matches network" — they are completely different (different ID schemes, different component counts, Pfafstetter offset vs per-region) | Verify claims against actual data before documenting. `network` is v17b original, `subnetwork_id` is v17c with Pfafstetter offset |
| 2026-03-13 | self | NetCDF export used DuckDB reach x/y/x_min/x_max/y_min/y_max — but DuckDB geometries lack overlap vertices so centroids differ from v17b by up to 8.7 km (17,448 reaches > 100m off) | Copy reach coordinate columns from v17b NetCDF directly. Node coords are fine (exact match). Centerlines are fine (copied from DuckDB which matches v17b) |
| 2026-03-14 | self | Audit worker reported only 16 topology mismatches (AS:14, EU:2) — actual count was 389 across 5 regions. Worker checked a subset of conditions | Don't trust audit worker counts without independent verification. Cross-check with full-region scans |
| 2026-03-14 | self | Flow correction oscillation: 389 reaches flipped back and forth across 5 pipeline runs because WSE slope signal is ambiguous. rch_id_dn_main computed from pre-flip graph but topology table had post-flip state. No cross-run oscillation guard | Flow correction needs a persistent oscillation guard (not just per-run). For ambiguous sections, trust v17b. Reverted 389 reaches' topology to v17b backup |

## Mistakes This Session (v17c NetCDF export)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-13 | self | Assumed v17b NetCDF rows are sorted by ID (reach_id, node_id, cl_id). They are NOT — v17b uses its own ordering. | Always read v17b IDs first and build a reorder index from DuckDB (sorted) → v17b order |
| 2026-03-13 | self | DuckDB `fetchnumpy()` returns `MaskedArray` with `MaskedConstant` for NULLs. `MaskedConstant` is not hashable and fails `v in mapping` | Use `_is_valid()` helper that checks for None, MaskedConstant, and NaN before dict lookup |
| 2026-03-13 | self | Tried `v._FillValue = "-9999.0"` after `createVariable()` for string vars. NetCDF4 requires fill_value at creation time | Pass `fill_value=` kwarg to `createVariable()`, never set `_FillValue` as attr |
| 2026-03-13 | self | `gsutil -m cp` hangs on macOS due to multiprocessing bug | Use sequential `gsutil cp` without `-m` flag, or add `-o "GSUtil:parallel_process_count=1"` |
| 2026-03-13 | self | Reviewer fix JSON no-ops (old=0, new=0 from undo/redo) would overwrite current value | Filter fixes where `old_value == new_value` before applying |
| 2026-03-13 | self | Applied reviewer lakeflag fixes to reaches only, forgot nodes inherit lakeflag from parent reach | Always propagate shared columns (lakeflag) to nodes when fixing reaches — 2,503 nodes needed updating |
| 2026-03-13 | self | Claimed "all error lint checks pass" without running them — user challenged, found 5 distinct failures | Always run `--severity error --fail-on-error` lint and parse the JSON before claiming clean |
| 2026-03-13 | self | V010 lint check treated -9999 fill value as FK reference, flagging 171 false positives across all regions | Lint checks must always exclude fill values (-9999) from referential integrity checks |
| 2026-03-13 | self | Re-ran v17c pipeline for OC which flipped 26 topology sections, then tried to restore backup — user wanted the flips kept | Ask before reverting pipeline changes. SWOT-validated flips are improvements, not regressions |
| 2026-03-13 | self | Edited lint check in worktree file but Python loads from main repo `/Users/jakegearon/projects/SWORD/src/` | Always check `module.__file__` to confirm which source Python actually loads before editing |
| 2026-03-13 | self | Initial boundary gap query for #189 used bounding box corners (x_min/y_min, x_max/y_max) instead of geometry endpoints — got 152K false matches | Use ST_StartPoint/ST_EndPoint or node coordinates for endpoint distance, not bbox corners |

## Mistakes This Session (docs review)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-16 | gemini-worker | Gemini docs-review worker claimed `hydro_dist_hw` was not in the export or pipeline — recommended deleting it from docs. It IS at line 154 of export_netcdf.py and populated for all 248K reaches | Always cross-check worker findings against the actual code before merging. Don't trust worker deletions of documented variables |
| 2026-03-16 | self | Described DuckDB coordinate drift as "working-database artifact only" in release notes — user corrected: DuckDB should match v17b, we fixed this before | DuckDB coordinates must match v17b. If they drift, fix the data, don't document the drift as acceptable |
| 2026-03-16 | self | RTREE index drop failed because `duckdb_indexes()` returns indexes from ALL attached databases (v17b + v17c). Tried to drop v17b's index on v17c connection | Filter `duckdb_indexes()` by `database_name` when multiple databases are attached, or query indexes BEFORE attaching other databases |

## Mistakes This Session (HarP lake corrections + exports)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-17 | self | `load_from_duckdb.py` truncated tables in forward order (parents before children) — FK constraint violation on `sword_operations` → `sword_value_snapshots`. Only NA loaded; SA-OC aborted | Truncate in REVERSE table order (children first), load in forward order (parents first). FK deps matter |
| 2026-03-17 | self | `facc_corrections` in DuckDB had duplicate PKs: batch 2 was exact copy of batch 1, batch 3 had NULL correction_ids. DuckDB doesn't enforce PK uniqueness | Always verify PK uniqueness manually in DuckDB before PG load. DuckDB PKs are advisory only |
| 2026-03-17 | self | Tried to launch dgov gemini worker while on `harp-lake-corrections` branch and with unstaged changes | dgov requires main branch + clean working tree. `git checkout main && git stash` first |
| 2026-03-17 | self | Used Agent subagent for Gemini code review instead of dgov CLI | Use `dgov pane create -a gemini` for code reviews, not Agent tool |
| 2026-03-17 | gemini-worker | Gemini PR review flagged DuckDB `UPDATE ... .fetchone()[0]` as P1 bug (claimed it returns None). It returns affected row count — standard DuckDB behavior | DuckDB `.execute('UPDATE ...').fetchone()[0]` returns row count. This is correct, not a bug |
| 2026-03-18 | POM | `n_rch_up`/`n_rch_down` didn't match actual topology for 148 reaches. Flow corrections flipped direction in `reach_topology` but never updated scalar counts | Any pipeline step that modifies topology direction must also recalculate `n_rch_up`/`n_rch_down` from `reach_topology` |
| 2026-03-18 | self | Google Drive GPKG files (Mar 16) had only nodes, no reaches. Shared stale files from a broken export | Always verify shared exports have all expected layers before sharing. Check with `fiona.listlayers()` |
| 2026-03-18 | self | Google Drive NetCDF files had stale checksums — older than local `data/exports/v17c_beta/` | After any re-export, always sync to Google Drive AND update SHA256SUMS.txt |
| 2026-03-18 | self | Claimed 151 non-reciprocal pairs were "inherited from v17b" — v17b actually has ZERO. They were v17c flow correction regressions | Always verify claims against v17b data before documenting as inherited |
| 2026-03-18 | self | Previous session panicked about "bad topology fix" — but the fix was actually correct. The oscillation revert policy says all non-OC flow corrections should match v17b. The 151 reciprocal entries were residual from incomplete oscillation revert. Completing them to v17b was correct | Verify database state empirically before assuming a fix was wrong. The reasoning can be wrong while the result is right |
| 2026-03-18 | self | AS NetCDF+GPKG exports were made before reciprocity fix (19 stale reaches). NA GPKG was truncated (760K/1.7M nodes, journal file). Other 4 regions fine | Always verify exports match DB after any topology change. Check n_rch + layer counts |
| 2026-03-18 | self | Node region detection by first digit of node_id is WRONG — AS spans prefixes 3, 4, 8, 9. Use the `region` column on the nodes table instead | Never detect region from ID prefix. Use the explicit region column |

## Mistakes This Session (prior)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-02 | review | fix_centerline_node_assignment.py had Euclidean-degree ORDER BY for nearest-node but Haversine-approx for detection — would pick wrong node at high latitudes | Always use the same distance formula in ORDER BY as in WHERE filtering |
| 2026-03-02 | self | Used `rm` which is blocked by hooks | Use `trash` for file deletion |
| 2026-03-02 | self | Assumed #187 (DuckDB missing overlap vertices) was real without verifying — wrote a whole fix script before checking | Always verify the premise with data before writing fix code |

## Mistakes This Session (reviewer persistence)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-10 | bug | Reviewer reviewed-reach state only read from DuckDB lint_fix_log (ephemeral on Cloud Run). On container restart, reviews lost if JSON replay failed | Always merge both DB and JSON sources for reviewed reaches (`get_reviewed_reaches` helper) |
| 2026-03-10 | bug | JSON session writes used `open("w")` — truncates before writing. Container kill mid-write = corrupted/empty JSON = all reviews lost on next replay | Atomic writes: temp file + fsync + rename |
| 2026-03-10 | bug | `load_session_fixes` had no try/except — corrupted JSON crashes app instead of returning empty defaults | Always handle `json.JSONDecodeError` and `OSError` |
| 2026-03-10 | self | Hook false positive: `conflict-marker-guard.sh` matches `^={7}` in RST-style docstring underlines (`==================`) | Replace `====` underlines with `----` in docstrings |

## Mistakes This Session (continued)
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-03-03 | self | `--skip-flow-correction` CLI flag never wired — code uses `not args.enable_flow_correction` which defaults True, so flow correction always runs regardless of --skip flag | Pre-existing bug, use Python API `skip_flow_correction=True` directly |
| 2026-03-03 | self | `compute_main_neighbors` used 2-tuple `(width, facc)` while `compute_best_headwater_outlet` used 3-tuple `(width, facc, pathlen+reach_length)` — caused 1,000 V002 divergences at width/facc-tied bifurcations | Always use identical ranking tuples across all routing functions: `(effective_width, log_facc, pathlen + reach_length)` |
| 2026-03-03 | self | First tiebreaker fix used `pathlen_out` alone — still 1,000 violations because original uses `pathlen_out + reach_length` | Match the EXACT tiebreaker formula, not just the variable name |

## Domain Notes
- **pathlen_hw semantics**: Cumulative reach_lengths from headwater→self (self included, headwater excluded). Headwater gets 0. Computed in upstream topological pass.
- **pathlen_out semantics**: Cumulative reach_lengths from self's successor→outlet (all included, self excluded). Outlet gets 0. Computed in downstream topological pass.
- **pathlen_hw + pathlen_out = constant** along the same (best_headwater, best_outlet) path. Constant = total_path_length - headwater_reach_length.
- **pathlen_hw step violations (V016)**: 1,550 globally, ALL at confluences (n_rch_up > 1), 0 on 1:1 links. Caused by upstream pass picking a different best predecessor than rch_id_dn_main implies. Expected routing divergence.
- **pathlen_out step violations (V017)**: 0 globally. pathlen_out and rch_id_dn_main both computed in downstream direction, so they always agree.
- **V019 direction violations**: 737 total — 661 involve side channels (main_side > 0), 76 main-to-main. Side-channel violations are expected routing divergence at rejoins.

- **N013 (CL-node misallocation) is 99.7% resolved**: v17b had 89,364 violations, v17c has 311. Fixed by `sync_centerline_node_ids()` (commit 7b0ca71) using cl_id_min/max range reassignment. Remaining 311 are structural (node sparsity). Spatial-nearest fix evaluated but rejected — breaks cl_id_min/max contiguity on 26/59 reaches.
- **Pfafstetter continent codes for globally unique IDs**: AF=1, EU=2, AS=3, OC=5, SA=6, NA=7 (from first digit of reach_id). Shared constant in `src/sword_v17c_pipeline/pfaf_offsets.py`. Use `pfaf_offset(region)` to get offset (e.g. NA→7,000,000). Applied to main_path_id, path_segs, subnetwork_id. `network` (v17b from UNC) stays untouched.
- **subnetwork_id vs network**: Different variables. Both are weakly connected components but with different enumeration order (only 0-2% match). `network` is UNC's v17b original. `subnetwork_id` is our v17c computation — now globally unique with Pfafstetter offsets.
- **SWORD reused node IDs across versions** — same node_id can map to completely different rivers/coordinates between v17 releases. Always validate node coordinates spatially when joining across data sources. African rivers (ANJOBONY, BEMARIVO, ELE, V7_2, LILONGWE, SARARE) are affected; SA rivers are fine because their geometry didn't change.
- **Distance column semantics (post-split)**: `dist_out` = v17b original, `dist_out_dijkstra` = Dijkstra shortest to ANY outlet, `hydro_dist_out` = mainstem walk via rch_id_dn_main (includes own reach_length). `hydro_dist_hw` dropped (= pathlen_hw + reach_length). Mississippi spot-check: hydro=3,265km, dijkstra=3,039km (226km diff from mainstem routing). V001=0 violations, V002=0 violations globally (248,673 reaches).
- **Routing consistency rule**: All functions that pick downstream/upstream neighbors must use the same 3-tuple ranking: `(effective_width, log_facc, pathlen + reach_length)`. Functions: `compute_best_headwater_outlet`, `compute_main_neighbors`. If they diverge, `hydro_dist_out ≠ pathlen_out + reach_length` at width/facc-tied bifurcations.
- **Backwater override cascade danger**: `compute_best_headwater_outlet` propagates headwater identity downstream through every reach via topological sort. Forcing a different predecessor via override changes `pathlen_hw` at one junction, which cascades `best_outlet` selection for thousands of downstream reaches. `compute_main_neighbors` is LOCAL (only sets that node's up/dn main) so overrides are safe there. NEVER re-add overrides to `compute_best_headwater_outlet` — the parameter was deliberately removed.
- **OC topology flips (2026-03-13)**: v17c pipeline flipped 26 sections based on SWOT slope validation (180 links, 116 reaches). dist_out recomputed iteratively from boundary reaches (11 iterations). Backup: `reach_topology_backup_OC_3811541ae0d6`. Issue #200.
- **Flow correction oscillation (2026-03-14)**: 389 reaches in AF/AS/EU/NA/SA have ambiguous WSE slope — flow correction flips them every pipeline run. Root cause: scoring detects both directions as "wrong." Fix: reverted topology to v17b (first backup) for all oscillating sections. rch_id_dn_main already matched v17b direction. Need persistent oscillation guard for production (track flip_history across runs, not just within one run).
- DB files live at `/Users/jakegearon/projects/SWORD/data/duckdb/`, NOT in worktree paths. DuckDB files are symlinked or accessed from main repo.
- PostgreSQL sword_v17c schema v1.7.0 matches DuckDB v17c as of 2026-02-23 (percentile SWOT columns, dn_node_id/up_node_id, node_order, topology_suspect/approved, facc_corrections, v17c_flow_corrections)
- MCP PostgreSQL tool connects to `postgres` db, not `sword_v17c` — use psql CLI for sword_v17c queries
- v17b geometry overwrite via dblink works: 248,673 reach geometries replaced with v17b originals for endpoint connectivity
- path_freq monotonicity violations (448 total) are ALL side-channel rejoin edges (main_side=1 or 2). v17b had 1,536 — v17c improved this 3x
- Issue #16 (path_freq=0/-9999 on connected reaches) is already fixed. All 23K nodata values are ghost reaches (type=6)
- `deploy/reviewer/lint/` has a separate copy of lint checks — must keep in sync with `src/sword_duckdb/lint/`
- Pre-existing ruff warning in `deploy/reviewer/lint/checks/topology.py:432` — unused `total_reaches` variable in T006
